#pragma once

// ─── clk2hzSetup ─────────────────────────────────────────────────────────────
// Налаштовує Timer5 у режимі CTC для генерації сигналу 2 Гц на CLK2HZ_PIN (46).
// Ехо-сигнал виводиться на CLK_ECHO1_PIN (34) та CLK_ECHO2_PIN (35) через ISR.
// Викликати одного разу з setup() — сигнал активний одразу після виклику.
void clk2hzSetup();
void clk2hzUpdate();  // викликати кожен loop()
